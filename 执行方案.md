# FineJob 执行方案

## 1. 文档定位

本文件是根目录执行方案。test.md 是本方案的完整设计依据，未修改其中的角色、消息、状态和流程。

执行时遵循以下边界：

- test.md 已经规定的行为直接作为目标行为，不重新设计
- 当前代码和其他 Markdown 文档只用于核对实现现状，统一列在“当前项目实现差异”中。
- test.md 未涉及、且会改变运行行为的内容列在“待确认项”中。
- 插件没有队首任务概念。插件维护后端发送的完整队列，并根据当前 BOSS 页面匹配队列任务。
- 插件执行完成后发送完成消息；后端收到消息后负责关闭对应任务页面。
- 页面关闭动作必须先于下一轮任务页面打开；只发起关闭动作，不记录或监听页面是否关闭成功。

## 2. test.md 设计基线（历史摘录）

本节保留方案形成时的设计摘录。实际执行基线以根目录 test.md 的最新内容为准；本节与第 4、6、9、10 节的已确认规则存在差异时，按后者执行。

# FineJob 执行任务流程
## 概括
su，文档流程概括如下：
1. FineJob 后端与浏览器插件连接成功后，FineJob 后端发送完整任务队列，FineJob 页面开始轮询状态。
2. 浏览器插件收到队列后，浏览器插件更新本地任务队列状态。
3. 浏览器插件检查当前 BOSS 页面是否属于任务队列页面。
4. 页面匹配时，浏览器插件发送执行任务 ID，FineJob 后端更新任务状态，FineJob 页面显示正在执行，浏览器插件执行任务。
5. 页面未匹配时，浏览器插件请求 FineJob 后端打开任务页面，浏览器插件不指定具体任务；FineJob 后端打开页面并发送结果，浏览器插件开始轮询检查页面。
6. 浏览器插件连续 x 次检查失败后，浏览器插件发送心跳并重新请求打开页面；FineJob 后端打开下一个页面。
7. 浏览器插件再次连续 x 次检查失败后，浏览器插件发送执行错误，FineJob 后端记录错误，浏览器插件断开连接。
8. 浏览器插件执行成功后，浏览器插件发送成功信息并同时开始下一轮匹配；FineJob 后端关闭对应页面、更新任务和队列、发送完整队列；FineJob 页面显示任务成功。
9. FineJob 页面只在 FineJob 后端与浏览器插件匹配时轮询状态；FineJob 后端与浏览器插件匹配成功后，FineJob 页面重新开始轮询；用户点击刷新按钮后，FineJob 页面主动请求一次状态。


## 一、角色定义

### FineJob 后端

FineJob 后端负责维护执行任务队列，保存任务运行状态，向浏览器插件发送完整执行任务队列，接收浏览器插件发送的任务匹配信息、心跳信息和执行结果，打开任务执行页面，关闭任务执行页面，并提供 FineJob 页面查询运行状态的接口。

### 浏览器插件

浏览器插件运行在 BOSS 浏览器中。浏览器插件只负责浏览器侧的任务执行和状态显示。浏览器插件在执行任务过程中接收完整执行任务队列、读取当前 BOSS 页面、检查当前页面，并向 FineJob 后端发送任务匹配信息、打开页面请求、心跳信息和执行结果。

### FineJob 页面

FineJob 页面是用于显示执行任务队列和任务运行状态的桌面端页面。

FineJob 页面通过状态接口向 FineJob 后端请求以下数据：

- FineJob 后端与浏览器插件的匹配状态。
- 当前正在执行的任务。
- 执行任务队列及其状态。
- 任务执行成功、失败和异常信息。

## 二、两种匹配关系

### FineJob 后端与浏览器插件的匹配

FineJob 后端与浏览器插件的匹配表示 FineJob 后端与浏览器插件连接成功并可以进行任务执行通信。

FineJob 后端与浏览器插件连接成功后，FineJob 页面开始轮询请求 FineJob 后端的状态。

FineJob 页面在 FineJob 后端与浏览器插件处于匹配状态时，FineJob 页面继续轮询请求 FineJob 后端的状态。

FineJob 后端与浏览器插件匹配成功后，FineJob 页面重新开始轮询请求 FineJob 后端的状态。

### 浏览器插件与任务页面的匹配

浏览器插件与任务页面的匹配表示浏览器插件检查到当前 BOSS 页面属于执行任务队列中的任务页面。

浏览器插件与任务页面匹配后，浏览器插件读取当前页面对应的岗位 ID，确定对应的执行任务 ID，并把执行任务 ID发送给 FineJob 后端。

## 三、FineJob 后端与浏览器插件发送的消息

FineJob 后端通过 WebSocket 向浏览器插件发送完整执行任务队列和更新后的完整执行任务队列。

FineJob 后端完成打开任务页面动作后，FineJob 后端向浏览器插件发送页面打开结果和对应任务信息。

浏览器插件向 FineJob 后端发送打开任务页面请求、匹配上的执行任务 ID、心跳信息、执行成功消息和执行错误信息。

FineJob 页面通过状态接口向 FineJob 后端请求匹配状态、任务队列和任务运行状态。

## 四、FineJob 后端发送完整执行任务队列

FineJob 后端与浏览器插件连接成功后，FineJob 后端通过 WebSocket 向浏览器插件发送完整执行任务队列。

执行任务队列发生变化时，FineJob 后端通过 WebSocket 向浏览器插件发送更新后的完整执行任务队列。

执行任务队列发生变化包括以下情况：

- FineJob 后端新增执行任务。
- FineJob 后端删除执行任务。
- FineJob 后端修改执行任务信息。
- FineJob 后端修改执行任务状态。
- FineJob 后端修改执行任务顺序。
- FineJob 后端记录执行任务成功。
- FineJob 后端记录执行任务失败。

浏览器插件收到完整执行任务队列后，浏览器插件更新本地任务队列状态。

## 五、浏览器插件开始执行任务

浏览器插件开始执行任务时，浏览器插件读取当前 BOSS 页面。

浏览器插件判断当前 BOSS 页面是否属于执行任务队列中的任务页面。

### 当前 BOSS 页面属于执行任务队列中的任务页面

浏览器插件从当前 BOSS 页面读取岗位 ID。

浏览器插件根据岗位 ID确定对应的执行任务 ID。

浏览器插件向 FineJob 后端发送匹配上的执行任务 ID。

FineJob 后端收到执行任务 ID后，FineJob 后端更新对应执行任务的运行状态。

FineJob 页面通过状态接口请求 FineJob 后端后，FineJob 页面显示当前正在执行的任务类型、岗位名称、公司名称和岗位 ID。

浏览器插件执行当前页面对应的任务。

### 当前 BOSS 页面不属于执行任务队列中的任务页面

浏览器插件向 FineJob 后端发送打开任务页面请求。

浏览器插件发送的打开任务页面请求表示打开要执行的任务页面，浏览器插件不指定具体任务。

FineJob 页面通过状态接口请求 FineJob 后端后，FineJob 页面显示“正在匹配执行任务”。

FineJob 后端收到打开任务页面请求后，FineJob 后端打开某个任务执行页面。

FineJob 后端完成打开任务页面动作后，FineJob 后端向浏览器插件发送页面打开结果和对应任务信息。

浏览器插件收到 FineJob 后端发送的页面打开结果后，浏览器插件开始轮询检查当前 BOSS 页面。

## 六、浏览器插件轮询检查任务页面

浏览器插件按照设定的检查间隔读取当前 BOSS 页面。

浏览器插件每次检查当前 BOSS 页面是否属于执行任务队列中的任务页面。

浏览器插件在轮询检查期间只判断当前页面是否属于执行任务队列中的任务页面。

浏览器插件识别到当前 BOSS 页面属于执行任务队列中的任务页面后，浏览器插件读取岗位 ID并确定对应的执行任务 ID。

浏览器插件结束本轮轮询检查。

浏览器插件向 FineJob 后端发送匹配上的执行任务 ID。

FineJob 后端收到执行任务 ID后，FineJob 后端更新对应执行任务的运行状态。

FineJob 页面通过状态接口请求 FineJob 后端后，FineJob 页面显示当前正在执行的任务信息。

浏览器插件执行对应任务。

## 七、浏览器插件达到页面检查次数限制

浏览器插件达到限定的 `x` 次检查后，浏览器插件仍未识别到属于执行任务队列中的任务页面。

浏览器插件向 FineJob 后端发送心跳信息。

浏览器插件再次向 FineJob 后端发送打开任务页面请求。

浏览器插件发送的打开任务页面请求表示打开要执行的任务页面，浏览器插件不指定具体任务。

FineJob 后端收到新的打开任务页面请求后，FineJob 后端打开下一个任务页面。

FineJob 后端完成打开动作后，FineJob 后端向浏览器插件发送新的页面打开结果和对应任务信息。

浏览器插件收到新的页面打开结果后，浏览器插件重新开始轮询检查当前 BOSS 页面。

浏览器插件再次达到限定的 `x` 次检查后，浏览器插件仍未识别到属于执行任务队列中的任务页面。

浏览器插件向 FineJob 后端发送执行错误信息。

FineJob 后端记录对应执行任务的执行错误。

FineJob 页面通过状态接口请求 FineJob 后端后，FineJob 页面显示任务执行错误。

浏览器插件发送执行错误信息后，浏览器插件断开与 FineJob 后端的连接。

## 八、浏览器插件执行成功

浏览器插件执行成功后，浏览器插件向 FineJob 后端发送执行成功消息。

浏览器插件发送的执行成功消息包括：

- 执行任务 ID。
- 任务类型。
- 岗位名称。
- 公司名称。
- 岗位 ID。
- 执行结果。
- 平台返回信息。
- 执行完成时间。

FineJob 后端收到执行成功消息后，FineJob 后端发起关闭对应执行任务页面动作。

FineJob 后端更新对应执行任务的成功状态，并从待执行队列移除对应任务。

FineJob 后端打开下一任务执行页面。

FineJob 后端完成下一任务页面打开动作后，通过 WebSocket 向浏览器插件发送更新后的完整执行任务队列和页面打开结果。

浏览器插件收到更新后的完整执行任务队列后，浏览器插件更新本地任务队列状态；收到页面打开结果后，开始下一轮任务页面匹配和任务页面打开流程。

FineJob 页面通过状态接口请求 FineJob 后端后，FineJob 页面显示对应任务执行成功。

## 九、FineJob 页面状态请求流程

FineJob 后端与浏览器插件连接成功后，FineJob 页面开始轮询请求 FineJob 后端的状态接口。

FineJob 页面在 FineJob 后端与浏览器插件处于匹配状态时，FineJob 页面继续轮询请求 FineJob 后端的状态接口。

FineJob 后端与浏览器插件匹配成功后，FineJob 页面重新开始轮询请求 FineJob 后端的状态接口。

FineJob 页面通过状态接口请求获取 FineJob 后端保存的当前执行任务、执行任务队列、FineJob 后端与浏览器插件的匹配状态、任务运行结果和异常信息。

FineJob 页面根据 FineJob 后端状态接口返回的数据显示“正在匹配执行任务”“正在执行”“执行成功”和“执行错误”等任务运行状态。

用户点击刷新按钮后，FineJob 页面立即请求一次 FineJob 后端的状态接口。

## 十、执行任务队列数据

每个执行任务包含以下数据：

- 唯一任务 ID。
- 任务类型，例如打招呼、代聊回复。
- 打开任务页面的信息。
- 岗位名称。
- 公司名称。
- 岗位 ID。
- 执行内容。
- 任务状态。
- 执行结果。
- 平台返回信息。
- 错误信息。
- 创建时间。
- 更新时间。

## 3. 当前项目实现核对与差异

以下内容是对当前项目代码和现有文档的核对结果，属于实现现状，不改变上面的设计基线。

### 3.1 后端现状

- backend/app/services/fine_job/boss_executor.py 已有插件配对、执行器状态、心跳、队列查询、打开页面、任务匹配和完成任务服务。
- fj_automation_actions 已保存任务状态、execution_epoch、结果信息等字段；当前可见状态包括 queued、running、succeeded、failed、blocked、unknown、cancelled。
- list_queue 当前按 created_at、id 返回任务，队列序列化内容还没有覆盖 test.md 要求的全部执行内容、平台返回信息等字段。
- WebSocket 通道已经存在，但当前消息处理主要覆盖 heartbeat_test_result，没有实现完整队列推送和任务运行消息处理。
- open_task_page() 中存在一条只取一条 queued 任务的 SQL，位置约为 362～368 行。该 LIMIT 1 没有 ORDER BY，不代表队首，也不能推出插件存在队首概念。
- open_navigation() 已记录 fj_boss_navigation_tasks、目标地址和 browser_target_id，并调用现有导航打开能力；当前没有成功后的页面关闭动作。
- match_task() 能更新任务为 running，但尚未完成页面目标与一次执行流程的完整关联。
- complete_task() 能保存执行结果，但当前没有串联页面关闭、队列更新广播和下一轮页面打开。
- 后端启动时会重置浏览器连接状态，现有连接状态和风险状态逻辑需要与新的运行消息流程衔接。

### 3.2 浏览器插件现状

- boss-executor-extension/src/finejob/client.ts 已有配对、REST 心跳、控制 WebSocket 和结果上报。
- 当前控制 WebSocket 主要处理 heartbeat_test，插件没有接收完整任务队列的运行时订阅。
- 插件结果上报当前调用任务完成 REST 接口，待上报结果主要在后续启动或心跳时再次提交。
- 插件没有调用完整队列、打开页面、任务匹配等执行流程接口，也没有实现 test.md 规定的本地队列调度。
- content-service、content.ts 和 entrypoints/boss/index.ts 已有主世界命令转发和执行结果回传能力，可以作为队列调度器的执行出口。
- read-only-probe.ts 已具备 BOSS 页面快照能力，能够识别页面类型、登录状态、岗位标识和页面是否可执行。该能力应作为页面匹配基础，不新增另一套页面识别逻辑。
- default-greeting.ts 已能执行一次默认打招呼并产生执行结果；代聊回复仍属于独立的聊天执行路径，需要接入同一套任务调度消息。

### 3.3 FineJob 页面现状

- apps/desktop/src/renderer/stores/fineJobBossExecutor.ts 已有执行器状态、配对、控制、心跳测试、断开和手动打开页面能力。
- ReviewQueue.vue 和 DeliveryRunStatus.vue 已有状态请求和队列展示，但当前状态请求周期并非只在后端与插件匹配时运行。
- 当前页面有手动打开岗位页面和手动返回复核流程，尚未展示 test.md 规定的自动匹配、当前执行任务和完整运行结果链路。
- 现有状态模型可以继续承载任务状态；页面展示状态需要根据后端返回的匹配状态、当前任务和错误信息统一映射。

### 3.4 其他文档现状

- README.md、任务交接.md、自动代聊功能具体执行方案.md、Codex与FineJob业务能力集成执行方案.md 对默认打招呼、代聊和执行器的完成程度描述不完全一致。
- 这些文档不能覆盖或修改 test.md 的角色、消息、状态和流程。本方案只把其中与当前代码对应的内容作为实现背景，不把文档中的“已完成”描述当作本流程已经实现的依据。

## 4. 目标架构

### 4.1 组件关系

    FineJob 页面
        │ REST 状态请求
        ▼
    FineJob 后端 ─────── WebSocket 运行通道 ─────── 浏览器插件
        │                                          │
        │ 页面打开/关闭动作                         │ 读取 BOSS 页面快照
        ▼                                          ▼
    BOSS 浏览器导航能力                         BOSS 页面执行器

后端是任务队列、任务状态和执行流程的权威来源。插件保存后端下发的完整队列副本，用于本地页面匹配；FineJob 页面只通过状态接口展示后端状态。

### 4.2 任务执行主链路

1. 后端创建任务时生成唯一任务 ID；插件完成配对并建立 WebSocket 运行通道。
2. 后端立即发送完整任务队列，插件替换本地队列。
3. 插件读取当前 BOSS 页面快照并判断页面是否属于队列任务。
4. 匹配成功时，插件发送执行任务 ID；该消息用于后端更新任务状态和桌面端展示，插件随后执行任务。
5. 未匹配时，插件发送不带具体任务 ID 的打开任务页面请求；后端按确定后的队列顺序选择要打开的任务页面。
6. 页面打开完成后，后端发送页面打开结果和任务信息；插件从收到该通知时开始按配置间隔检查页面。
7. 第一次达到 x 次未匹配时，插件发送心跳并再次请求打开页面；后端打开下一个页面并通知插件，插件将检查次数清零。
8. 第二次达到 x 次未匹配时，插件发送执行错误；后端记录错误，已打开页面保留，插件断开连接。
9. 执行成功时，插件发送固定成功字段；后端依次发起当前页面关闭、打开下一任务页面，并在打开动作完成后向插件发送更新后的完整队列和页面打开结果。
10. 插件执行失败时，插件发送任务失败信息；后端更新失败状态、从待执行队列移除该任务并发送更新后的完整队列，当前页面保留。

test.md 中“同时开始下一轮匹配”保留为流程语义，具体执行顺序按本方案落实为：插件可以开始下一轮调度准备，但后端必须先发起当前页面关闭动作，之后才允许打开下一轮任务页面。

## 5. REST 与 WebSocket 分工

### 5.1 WebSocket：插件运行时通道

WebSocket 用于需要保持插件运行上下文和即时推送的运行消息：

- 后端发送完整任务队列。
- 后端发送更新后的完整任务队列。
- 后端发送页面打开结果和对应任务信息。
- 插件发送打开任务页面请求。
- 插件发送匹配上的执行任务 ID。
- 插件发送心跳信息。
- 插件发送执行成功消息。
- 插件发送任务失败消息。
- 插件发送执行错误信息。

这样可以让连接建立、队列同步、页面打开反馈、任务结果和下一轮调度处于同一条执行通道中。

### 5.2 REST：配对、页面查询和管理操作

REST 用于明确的请求响应和桌面管理操作：

- 插件配对、凭证建立和初始连接。
- FineJob 页面状态接口。
- FineJob 页面执行器控制、断开和人工管理操作。
- 现有桌面端心跳测试接口。
- 现有人工打开岗位和返回复核接口。

现有插件任务 REST 接口不直接删除。迁移时由它们复用同一套后端任务服务，避免 REST 和 WebSocket 形成两套互相不一致的任务状态流；自动执行主流程以 WebSocket 消息为准。

## 6. 消息和接口执行约定

### 6.1 WebSocket 消息类型与最小字段

| 方向 | 消息类型 | 最小字段 |
| --- | --- | --- |
| 后端 → 插件 | `task_queue` | 完整任务数组 |
| 后端 → 插件 | `page_opened` | 任务 ID、打开结果、页面信息 |
| 插件 → 后端 | `match_task` | 任务 ID |
| 插件 → 后端 | `open_task_page` | 不携带任务 ID |
| 插件 → 后端 | `heartbeat` | 心跳时间 |
| 插件 → 后端 | `task_succeeded` | 任务 ID、任务类型、岗位名称、公司名称、岗位 ID、执行结果、平台返回信息、完成时间 |
| 插件 → 后端 | `task_failed` | 任务 ID、失败信息、失败时间 |
| 插件 → 后端 | `execution_error` | 当前任务 ID、错误信息、发生时间 |

### 6.2 后端发送完整队列

连接建立成功后发送一次完整队列。以下事件发生后重新发送完整队列：

- 新增、删除、修改任务。
- 修改任务状态。
- 修改任务顺序。
- 记录任务成功或失败。

插件收到队列后整体更新本地队列，不通过增量事件自行推导后端状态。

### 6.3 打开任务页面

插件发送打开任务页面请求时不携带具体任务 ID。后端执行以下动作：

1. 根据队列状态和确定后的排序规则选取待打开任务。
2. 创建或更新导航任务记录。
3. 调用现有 BOSS 导航能力打开页面。
4. 返回页面打开结果和对应任务信息。

打开动作失败时，后端记录执行错误并向插件发送错误结果；已打开页面保留，插件和后端停止当前执行链，不进入下一轮。

### 6.4 页面匹配

插件使用 read-only-probe.ts 输出的页面快照作为判断输入：

- 页面是否属于 BOSS 任务页面。
- 页面是否已准备完成。
- 当前岗位 ID及其可验证标识。
- 当前页面是否处于可执行状态。

页面快照只用于页面归属和任务匹配。匹配成功后，插件读取岗位 ID并从本地完整队列确定执行任务 ID，再向后端发送该 ID。该消息用于后端更新任务运行状态和桌面端展示，插件不等待额外确认消息。

### 6.5 执行成功

成功消息必须保留 test.md 固定字段：

- 执行任务 ID。
- 任务类型。
- 岗位名称。
- 公司名称。
- 岗位 ID。
- 执行结果。
- 平台返回信息。
- 执行完成时间。

后端收到成功消息后的顺序：

1. 校验任务 ID、任务状态和当前执行上下文。
2. 发起关闭对应任务页面动作。
3. 更新任务成功状态和结果字段，并从待执行队列移除该任务。
4. 选择并打开下一任务页面。
5. 页面打开动作完成后，向插件发送更新后的完整队列和页面打开结果。

页面关闭只要求发起关闭动作，不新增关闭成功状态、不查询关闭结果、不等待或记录页面是否已经消失。

### 6.6 插件执行失败

插件执行器返回失败时，插件发送 `task_failed` 消息。后端记录失败信息，将对应任务更新为 `failed`，从待执行队列移除该任务，并向插件发送更新后的完整队列。

当前页面保留。插件收到新队列后继续按主链路检查当前页面；当前页面没有可匹配任务时，插件发送无任务 ID 的打开任务页面请求。

### 6.7 执行错误

插件达到第二次 x 次页面检查限制后发送执行错误。后端记录任务错误和错误信息，FineJob 页面通过状态接口显示执行错误，插件发送错误消息后断开连接。

打开页面失败属于另一条错误分支：后端记录错误，保留已打开页面，当前任务链两端停止，不继续打开下一页面。

## 7. 状态和数据映射

### 7.1 test.md规定的页面显示状态

- 正在匹配执行任务。
- 正在执行。
- 执行成功。
- 执行错误。

这些显示状态属于设计基线，不因当前数据库枚举不同而改变。

### 7.2 现有后端任务状态

现有 fj_automation_actions 状态继续作为存储基础：

- queued：任务等待执行。
- running：后端收到匹配任务 ID并确认进入执行。
- succeeded：后端收到成功消息并保存成功结果。
- failed：后端记录执行错误或打开页面失败。
- blocked、unknown、cancelled：保留现有业务含义，页面显示为异常或对应业务状态。

匹配中的“正在匹配执行任务”应由后端当前运行上下文或导航任务状态表达，不把插件队首概念加入任务模型。

### 7.3 任务数据

任务接口和队列消息需要覆盖 test.md 中的唯一任务 ID、任务类型、打开页面信息、岗位名称、公司名称、岗位 ID、执行内容、任务状态、执行结果、平台返回信息、错误信息、创建时间和更新时间。

测试类型任务使用 `TEST_DELAY`。该任务关联一个测试岗位，固定等待 3 秒完成；`close_page_after_completion` 是任务级字段，每个测试任务独立决定是否在成功后关闭页面。

## 8. 实施步骤

### 阶段一：统一后端任务服务和消息模型

- 保留现有配对、执行器状态、任务表和导航任务表。
- 为 WebSocket 增加完整队列发送能力。
- 为 WebSocket 增加打开页面、任务匹配、心跳、成功和错误消息处理。
- 让 WebSocket 消息和现有 REST 任务接口复用同一套状态更新服务。
- 按约定实现 `task_queue`、`page_opened`、匹配、页面打开、心跳、成功、失败和执行错误消息。
- 明确打开失败的错误记录和两端停止结果。

### 阶段二：补齐后端页面打开、关闭和队列广播

- 将打开页面请求改为不指定具体任务的调度请求。
- 按确认后的队列顺序选择任务页面。
- 沿用现有导航记录中的 browser_target_id 执行当前页面关闭动作。
- 将关闭动作、下一页面打开、队列和页面打开结果通知按既定顺序串联。
- 任务成功或失败后发送完整队列。

### 阶段三：实现插件本地队列和调度器

- 建立完整队列的本地状态。
- WebSocket 建立后接收并替换完整队列。
- 使用页面快照匹配任务，不增加队首任务状态。
- 未匹配时发送无任务 ID的打开页面请求。
- 按配置记录连续页面检查次数。
- 插件开始页面检查时开始计数；后端打开新页面并通知插件后清零计数。
- 达到第一次 x 次时发送心跳并重新请求打开页面。
- 达到第二次 x 次时发送错误并断开连接。
- 匹配成功后把执行任务 ID交给现有主世界执行器。
- 执行成功后发送固定成功消息；执行失败后发送任务失败消息。

### 阶段四：接入现有执行器

- 复用 default-greeting.ts 的页面校验和执行结果结构。
- 将任务类型映射到对应执行器；默认打招呼先接入现有执行能力。
- 代聊回复沿用现有聊天执行能力，接入统一任务消息和结果回传。
- 保留现有执行结果持久化能力，但统一成功、失败和异常消息的后端处理。

### 阶段五：调整 FineJob 页面

- 状态接口返回后端与插件匹配状态、当前任务、完整队列、结果和异常。
- 仅按 test.md 规定的匹配状态控制页面状态请求。
- 展示“正在匹配执行任务”“正在执行”“执行成功”“执行错误”。
- 保留用户点击刷新后主动请求一次状态的行为。
- 将自动流程状态与现有手动打开页面、执行器控制区分展示。

### 阶段六：验收闭环

按以下顺序核对完整链路：

1. 配对连接后插件收到完整队列。
2. 当前页面匹配时，插件发送任务 ID并进入执行。
3. 当前页面不匹配时，插件发送无任务 ID的打开页面请求。
4. 页面打开结果返回后，插件检查页面并最终匹配。
5. 第一次 x 次失败后，心跳、重新打开页面和重新检查顺序正确。
6. 第二次 x 次失败后，后端记录错误，插件断开，两端停止当前链路。
7. 成功消息来自插件，后端收到后执行关闭动作。
8. 关闭动作先于下一轮页面打开。
9. 成功或失败后，FineJob 页面和插件都能看到后端的完整队列状态。

### 当前实施进度

- 已实现默认打招呼任务的 WebSocket 完整队列下发、任务匹配、页面打开、心跳、成功、失败和页面检查错误消息处理。
- 已实现默认打招呼成功后的页面关闭动作、下一任务页面打开、完整新队列和页面打开结果通知顺序。
- 已实现插件本地队列更新、当前 BOSS 页面检查、2 秒间隔与两轮各 5 次的页面检查流程，以及默认打招呼命令分发。
- 已实现桌面端与插件的执行状态双向同步：桌面端开始或暂停时由插件确认后回传；插件连接、断开、开始、暂停及任务状态变化时，后端通过桌面状态通道立即推送最新状态。
- 代聊回复继续使用现有独立聊天执行通道，尚未并入默认打招呼任务队列。
- 已实现 5 个系统测试岗位的自动初始化、查询与编辑。岗位 ID只读，`encrypt_job_id` 与 `job_link` 可编辑；默认链接为 `https://www.zhipin.com/`。
- 已实现运行状态页的“新建测试任务”右侧抽屉、关联测试岗位选择和任务级页面关闭开关，以及测试岗位编辑弹窗。
- 已实现 `TEST_DELAY` 测试任务：后端打开关联测试岗位链接，插件等待 3 秒回传完成，后端按当前任务关闭开关处理页面。
- 已完成验证：后端执行器 API 测试 2 项通过，桌面端执行器状态管理测试 4 项通过，插件客户端测试 4 项通过，插件类型检查通过。`fineJobBossChat.ts` 的详情空值访问已修正，桌面端全量类型检查和生产构建已通过。
- 已修复待确认页切换路由时的 DOM 卸载异常：待确认状态筛选使用单选控件，避免 `ElTabPane` 卸载阶段访问空 DOM 节点；新的桌面端构建产物已包含该页面结构。
- 已兼容旧版评估记录：待确认接口会补齐缺失的评估列表字段，待确认页对摘要、置信度和详情字段使用安全回退，避免历史数据缺字段中断页面渲染。
- 已隔离系统测试岗位：测试任务继续在运行状态页展示，待确认查询统一排除 `is_test = 1` 的岗位，不向待确认页面返回测试数据。
- 已调整插件运行状态展示：等待 FineJob 打开页面、匹配任务页面、请求重新打开、执行任务、同步状态、同步失败和队列为空均显示为明确文案；不再显示本地“上次结果”。桌面端在队列为空时显示“当前没有待执行任务”。
- 已移除“允许并运行”、紧急停止和独立自动权限。桌面端与插件均以 `running`、`paused` 两种执行状态显示一个切换按钮；状态为运行时显示“暂停”，状态为暂停时显示“开始运行”。

## 9. 已确认配置与待确认项

以下内容包含已经确认的运行配置，以及 test.md 中尚未确定具体实现形式的事项。

### 9.1 页面检查配置（已确认）

次数和间隔均配置化，test.md 中的 x 继续作为参数名：

- 连续失败次数：5 次。
- 页面检查间隔：2 秒。
- 插件开始轮询页面是否属于执行任务队列页面时开始计数。
- 后端成功打开新页面并通知插件后，插件将检查次数清零。

### 9.2 后端队列顺序（已确认）

后端按任务进入执行队列的时间升序选择下一任务。现有默认打招呼任务在进入执行队列时创建，因此使用 `created_at` 升序；同一时间的任务按任务 ID 升序排列。

## 10. 不纳入待确认的已确定行为

- 不增加插件队首任务概念。
- 插件成功消息是后端成功处理的来源，后端不是凭空收到成功状态。
- 成功后由后端执行当前页面关闭动作。
- 关闭动作先于下一轮页面打开。
- 不记录或监听页面是否关闭成功。
- 打开页面失败记录错误，当前执行链两端停止。
- 页面连续检查失败按 test.md 的两段 x 次流程执行。
- 完成消息的发送方是插件，任务状态、队列和页面关闭动作的处理方是后端。
- 后端创建任务时生成唯一任务 ID。
- 匹配任务 ID消息用于后端状态更新和桌面端展示，插件发送后直接执行。
- 成功后的顺序是发起关闭当前页面、打开下一任务页面、向插件发送完整新队列和页面打开结果。
- 插件执行失败后，后端将失败任务从待执行队列移除并发送完整新队列，当前页面保留。
- 页面检查以 5 次、2 秒执行；开始检查时计数，收到后端已打开新页面的通知后清零。
- 打开页面失败和第二轮检查失败后均保留已打开页面。
- 后端按任务进入执行队列的时间升序选择下一任务。
- 测试任务固定等待 3 秒；是否关闭页面由每个测试任务单独设置。
