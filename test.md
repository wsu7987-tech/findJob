# FineJob 执行任务流程
## 概括
su，文档流程概括如下：
1. FineJob 后端与浏览器插件连接成功后，FineJob 后端发送完整任务队列，FineJob 页面开始轮询状态。
2. 浏览器插件收到队列后，浏览器插件更新本地任务队列状态。
3. 浏览器插件检查当前 BOSS 页面是否属于任务队列页面。
4. 页面匹配时，浏览器插件发送执行任务 ID，FineJob 后端更新任务状态，FineJob 页面显示正在执行，浏览器插件执行任务。
5. 页面未匹配时，浏览器插件请求 FineJob 后端打开任务页面，浏览器插件不指定具体任务；FineJob 后端打开页面并发送结果，浏览器插件开始轮询检查页面。
6. 浏览器插件连续 x 次检查失败后，浏览器插件发送心跳并重新请求打开页面；FineJob 后端打开下一个页面。
7. 浏览器插件再次连续 x 次检查失败后，浏览器插件发送执行错误，FineJob 后端记录错误，浏览器插件断开连接。
8. 浏览器插件执行成功后，浏览器插件发送成功信息并同时开始下一轮匹配；FineJob 后端关闭对应页面、更新任务和队列、发送完整队列；FineJob 页面显示任务成功。
9. FineJob 页面只在 FineJob 后端与浏览器插件匹配时轮询状态；FineJob 后端与浏览器插件匹配成功后，FineJob 页面重新开始轮询；用户点击刷新按钮后，FineJob 页面主动请求一次状态。


## 一、角色定义

### FineJob 后端

FineJob 后端负责维护执行任务队列，保存任务运行状态，向浏览器插件发送完整执行任务队列，接收浏览器插件发送的任务匹配信息、心跳信息和执行结果，打开任务执行页面，关闭任务执行页面，并提供 FineJob 页面查询运行状态的接口。

### 浏览器插件

浏览器插件运行在 BOSS 浏览器中。浏览器插件只负责浏览器侧的任务执行和状态显示。浏览器插件在执行任务过程中接收完整执行任务队列、读取当前 BOSS 页面、检查当前页面，并向 FineJob 后端发送任务匹配信息、打开页面请求、心跳信息和执行结果。

### FineJob 页面

FineJob 页面是用于显示执行任务队列和任务运行状态的桌面端页面。

FineJob 页面通过状态接口向 FineJob 后端请求以下数据：

- FineJob 后端与浏览器插件的匹配状态。
- 当前正在执行的任务。
- 执行任务队列及其状态。
- 任务执行成功、失败和异常信息。

## 二、两种匹配关系

### FineJob 后端与浏览器插件的匹配

FineJob 后端与浏览器插件的匹配表示 FineJob 后端与浏览器插件连接成功并可以进行任务执行通信。

FineJob 后端与浏览器插件连接成功后，FineJob 页面开始轮询请求 FineJob 后端的状态。

FineJob 页面在 FineJob 后端与浏览器插件处于匹配状态时，FineJob 页面继续轮询请求 FineJob 后端的状态。

FineJob 后端与浏览器插件匹配成功后，FineJob 页面重新开始轮询请求 FineJob 后端的状态。

### 浏览器插件与任务页面的匹配

浏览器插件与任务页面的匹配表示浏览器插件检查到当前 BOSS 页面属于执行任务队列中的任务页面。

浏览器插件与任务页面匹配后，浏览器插件读取当前页面对应的岗位 ID，确定对应的执行任务 ID，并把执行任务 ID发送给 FineJob 后端。

## 三、FineJob 后端与浏览器插件发送的消息

FineJob 后端通过 WebSocket 向浏览器插件发送完整执行任务队列和更新后的完整执行任务队列。

FineJob 后端完成打开任务页面动作后，FineJob 后端向浏览器插件发送页面打开结果和对应任务信息。

浏览器插件向 FineJob 后端发送打开任务页面请求、匹配上的执行任务 ID、心跳信息、执行成功消息、任务失败消息和执行错误信息。

FineJob 页面通过状态接口向 FineJob 后端请求匹配状态、任务队列和任务运行状态。

### WebSocket 消息类型与最小字段

| 方向 | 消息类型 | 最小字段 |
| --- | --- | --- |
| FineJob 后端 → 浏览器插件 | `task_queue` | 完整任务数组 |
| FineJob 后端 → 浏览器插件 | `page_opened` | 任务 ID、打开结果、页面信息 |
| 浏览器插件 → FineJob 后端 | `match_task` | 任务 ID |
| 浏览器插件 → FineJob 后端 | `open_task_page` | 不携带任务 ID |
| 浏览器插件 → FineJob 后端 | `heartbeat` | 心跳时间 |
| 浏览器插件 → FineJob 后端 | `task_succeeded` | 任务 ID、任务类型、岗位名称、公司名称、岗位 ID、执行结果、平台返回信息、完成时间 |
| 浏览器插件 → FineJob 后端 | `task_failed` | 任务 ID、失败信息、失败时间 |
| 浏览器插件 → FineJob 后端 | `execution_error` | 当前任务 ID、错误信息、发生时间 |

## 四、FineJob 后端发送完整执行任务队列

FineJob 后端与浏览器插件连接成功后，FineJob 后端通过 WebSocket 向浏览器插件发送完整执行任务队列。

执行任务队列发生变化时，FineJob 后端通过 WebSocket 向浏览器插件发送更新后的完整执行任务队列。

执行任务队列发生变化包括以下情况：

- FineJob 后端新增执行任务。
- FineJob 后端删除执行任务。
- FineJob 后端修改执行任务信息。
- FineJob 后端修改执行任务状态。
- FineJob 后端修改执行任务顺序。
- FineJob 后端记录执行任务成功。
- FineJob 后端记录执行任务失败。

浏览器插件收到完整执行任务队列后，浏览器插件更新本地任务队列状态。

## 五、浏览器插件开始执行任务

浏览器插件开始执行任务时，浏览器插件读取当前 BOSS 页面。

浏览器插件判断当前 BOSS 页面是否属于执行任务队列中的任务页面。

### 当前 BOSS 页面属于执行任务队列中的任务页面

浏览器插件从当前 BOSS 页面读取岗位 ID。

浏览器插件根据岗位 ID确定对应的执行任务 ID。

浏览器插件向 FineJob 后端发送匹配上的执行任务 ID。

FineJob 后端收到执行任务 ID后，FineJob 后端更新对应执行任务的运行状态。

FineJob 页面通过状态接口请求 FineJob 后端后，FineJob 页面显示当前正在执行的任务类型、岗位名称、公司名称和岗位 ID。

浏览器插件执行当前页面对应的任务。

浏览器插件发送匹配上的执行任务 ID后直接执行任务，该消息用于 FineJob 后端更新运行状态和 FineJob 页面展示。

### 当前 BOSS 页面不属于执行任务队列中的任务页面

浏览器插件向 FineJob 后端发送打开任务页面请求。

浏览器插件发送的打开任务页面请求表示打开要执行的任务页面，浏览器插件不指定具体任务。

FineJob 页面通过状态接口请求 FineJob 后端后，FineJob 页面显示“正在匹配执行任务”。

FineJob 后端收到打开任务页面请求后，FineJob 后端打开某个任务执行页面。

FineJob 后端完成打开任务页面动作后，FineJob 后端向浏览器插件发送页面打开结果和对应任务信息。

浏览器插件收到 FineJob 后端发送的页面打开结果后，浏览器插件开始轮询检查当前 BOSS 页面。

浏览器插件从开始轮询检查时开始计算页面检查次数。

## 六、浏览器插件轮询检查任务页面

浏览器插件按照设定的检查间隔读取当前 BOSS 页面。

浏览器插件每次检查当前 BOSS 页面是否属于执行任务队列中的任务页面。

浏览器插件在轮询检查期间只判断当前页面是否属于执行任务队列中的任务页面。

浏览器插件识别到当前 BOSS 页面属于执行任务队列中的任务页面后，浏览器插件读取岗位 ID并确定对应的执行任务 ID。

浏览器插件结束本轮轮询检查。

浏览器插件向 FineJob 后端发送匹配上的执行任务 ID。

FineJob 后端收到执行任务 ID后，FineJob 后端更新对应执行任务的运行状态。

FineJob 页面通过状态接口请求 FineJob 后端后，FineJob 页面显示当前正在执行的任务信息。

浏览器插件执行对应任务。

## 七、浏览器插件达到页面检查次数限制

浏览器插件达到限定的 `x` 次检查后，浏览器插件仍未识别到属于执行任务队列中的任务页面。

浏览器插件向 FineJob 后端发送心跳信息。

浏览器插件再次向 FineJob 后端发送打开任务页面请求。

浏览器插件发送的打开任务页面请求表示打开要执行的任务页面，浏览器插件不指定具体任务。

FineJob 后端收到新的打开任务页面请求后，FineJob 后端打开下一个任务页面。

FineJob 后端完成打开动作后，FineJob 后端向浏览器插件发送新的页面打开结果和对应任务信息。

浏览器插件收到新的页面打开结果后，浏览器插件重新开始轮询检查当前 BOSS 页面。

FineJob 后端成功打开新页面并通知浏览器插件后，浏览器插件将页面检查次数清零。

浏览器插件再次达到限定的 `x` 次检查后，浏览器插件仍未识别到属于执行任务队列中的任务页面。

浏览器插件向 FineJob 后端发送执行错误信息。

FineJob 后端记录对应执行任务的执行错误。

FineJob 页面通过状态接口请求 FineJob 后端后，FineJob 页面显示任务执行错误。

浏览器插件发送执行错误信息后，浏览器插件断开与 FineJob 后端的连接。

页面打开失败和第二次达到限定的 x 次检查失败后，已打开页面保留。

## 八、浏览器插件执行成功

浏览器插件执行成功后，浏览器插件向 FineJob 后端发送执行成功消息。

浏览器插件发送的执行成功消息包括：

- 执行任务 ID。
- 任务类型。
- 岗位名称。
- 公司名称。
- 岗位 ID。
- 执行结果。
- 平台返回信息。
- 执行完成时间。

FineJob 后端收到执行成功消息后，FineJob 后端关闭对应执行任务页面。

FineJob 后端更新对应执行任务的成功状态。

FineJob 页面通过状态接口请求 FineJob 后端后，FineJob 页面显示对应任务执行成功。

FineJob 后端从待执行队列移除对应任务，并打开下一任务执行页面。

FineJob 后端完成下一任务页面打开动作后，通过 WebSocket 向浏览器插件发送更新后的完整执行任务队列和页面打开结果。

浏览器插件收到更新后的完整执行任务队列后，浏览器插件更新本地任务队列状态。

浏览器插件收到页面打开结果后，开始下一轮任务页面匹配和任务页面打开流程。

## 九、浏览器插件执行失败

浏览器插件执行任务失败后，浏览器插件向 FineJob 后端发送任务失败信息。

FineJob 后端记录对应执行任务的失败信息，将任务状态更新为失败，并从待执行队列移除对应任务。

FineJob 后端通过 WebSocket 向浏览器插件发送更新后的完整执行任务队列。

当前 BOSS 页面保留。浏览器插件收到更新后的完整执行任务队列后继续检查当前页面；当前页面不属于任务队列页面时，浏览器插件发送打开任务页面请求。

## 十、FineJob 页面状态请求流程

FineJob 后端与浏览器插件连接成功后，FineJob 页面建立状态通道接收后端推送的状态更新。

桌面端开始或暂停后，后端向插件发送控制消息；插件完成状态切换后回传确认，后端再向桌面端推送确认状态。

插件侧开始、暂停、连接、断开及任务状态变化时，插件向后端同步，后端立即推送给桌面端。

FineJob 页面通过状态接口请求获取 FineJob 后端保存的当前执行任务、执行任务队列、FineJob 后端与浏览器插件的匹配状态、任务运行结果和异常信息。

FineJob 页面根据 FineJob 后端状态接口返回的数据显示“正在匹配执行任务”“正在执行”“执行成功”和“执行错误”等任务运行状态。

用户点击刷新按钮后，FineJob 页面立即请求一次 FineJob 后端的状态接口。

## 十一、执行任务队列数据

每个执行任务包含以下数据：

- 唯一任务 ID。
- 任务类型，例如打招呼、代聊回复。
- 打开任务页面的信息。
- 岗位名称。
- 公司名称。
- 岗位 ID。
- 执行内容。
- 任务状态。
- 执行结果。
- 平台返回信息。
- 错误信息。
- 创建时间。
- 更新时间。

FineJob 后端创建执行任务时生成唯一任务 ID。

测试类型任务的任务类型为 `TEST_DELAY`，关联测试岗位并保存任务级的“执行完成后关闭页面”开关。测试任务打开关联岗位的 `job_link` 后，浏览器插件等待 3 秒并发送成功消息；后端仅在该任务开关为开启时发起页面关闭动作。
