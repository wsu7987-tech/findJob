# FineJob 自动代聊功能具体执行方案

> 文档版本：第三版实施记录  
> 编写日期：2026-08-31  
> 适用范围：浏览器插件、FineJob 后端、桌面端自动代聊页面  
> 原始方案：`docs/自动代聊功能具体执行方案.md`  
> 当前状态：默认招呼执行已按 `test.md` 收敛；自动代聊一期实现记录保留；本轮清理后的测试与构建待重新验证

## 一、文档定位

本文件是在现有代码、原始方案和当前产品约束基础上形成的优化实施依据与结果记录。`docs/自动代聊功能具体执行方案.md`保留为第一版设计与实现记录，本文件记录本次一期已修复的问题、实际实现、测试门槛和验收结果。

本文件同时记录自动代聊和插件交互优化的计划与实施结果。默认招呼执行以根目录 `test.md` 为准。

## 二、业务目标

FineJob 的核心业务链路是：

1. 用户维护简历和求职意向。
2. 系统采集并评估岗位，用户在桌面端审核。
3. 已确认岗位生成自动打招呼执行任务，由浏览器插件按 BOSS 页面岗位身份匹配后执行。
4. 招聘方回复后，自动代聊监听新消息，生成可编辑草稿。
5. 用户在桌面端检查、修改并明确确认后，插件才向 BOSS 发送消息。
6. 系统保存会话、草稿、确认、发送状态和异常，供用户持续处理。

自动代聊的产品定位是“AI 辅助、人工确认、插件执行”。当前阶段不开放 AI 自动发送，不同步 BOSS 历史聊天记录，也不在发送结果不明确时自动重试。

## 三、已经锁定的约束

### 3.1 自动打招呼执行边界

自动打招呼按根目录 `test.md` 定义的任务流程执行：

- FineJob 后端发送完整执行任务列表并保存任务状态。
- 浏览器插件读取 BOSS 页面岗位身份，匹配后回传任务 ID。
- FineJob 后端根据匹配结果更新任务状态，插件执行默认招呼并回传结果。
- 页面打开、岗位匹配、执行结果和任务列表同步使用同一条任务链路。
- 失败和未知结果保留在任务记录中，由后端继续提供状态。

### 3.2 自动代聊安全边界

- 所有发送都需要用户针对当前草稿明确确认。
- 用户确认必须绑定 `based_on_message_id` 和 `session_version`，新消息到达后旧确认自动失效。
- 发送结果为 `unknown` 时只允许人工核对，不自动再次发送。
- 人工接管或暂停会话后，不再创建或执行新的发送动作。
- 不抓取和补齐 BOSS 历史聊天记录，桌面端持续显示此限制。
- 第一次真实 BOSS 发送只能使用用户指定的测试联系人，并在执行当时再次取得用户批准。

## 四、当前实现结论

### 4.1 已具备的能力

- 插件可通过 BOSS `chat` WebSocket 观察消息，并区分候选人、招聘方和自身消息方向。
- 后端已具备会话、消息、回复任务、发送动作、事件和领导页等基础模型。
- 桌面端已有自动代聊会话列表、会话详情、草稿编辑、重新生成、确认发送、暂停和人工接管入口。
- 已实现领导页选举，避免多个 BOSS 标签页同时消费发送动作。
- 已实现基于消息和会话版本的基础过期校验。
- 自动打招呼与自动代聊在执行命令类型上已有分支隔离。

### 4.2 本轮清理状态

- 默认招呼执行器已收敛为完整任务列表、岗位身份匹配、任务状态更新和结果回传。
- 默认招呼执行保留完整任务列表同步、岗位身份匹配、任务状态更新和结果回传。
- 自动代聊的消息观察、草稿确认、领导页和发送结果链路保持独立。
- 本轮未运行测试、类型检查或构建；后续验证需单独批准。

### 4.3 当前架构

```text
BOSS 页面
  │ WebSocket 新消息
  ▼
插件 Main World 观察器 ──► Content/Background 协调器
                                │ 事件上报、领导页租约、动作领取
                                ▼
                         FineJob 后端
                    ┌───────────┴───────────┐
                    │ 会话/消息/草稿/动作   │
                    │ AI 草稿生成与状态机   │
                    └───────────┬───────────┘
                                │ 查询、编辑、确认
                                ▼
                         Electron 桌面端

确认后的发送动作：后端 → 领导标签页 → Main World MQTT/Protobuf → BOSS
```

职责保持清晰：插件负责观察和执行，后端负责权威状态，桌面端负责用户审核与控制。

## 五、问题清单与处理优先级

### 5.1 P0：实施前必须解决

| 编号 | 问题 | 当前风险 | 方案 |
| --- | --- | --- | --- |
| P0-1 | 新消息先查询运行状态，再写本地事件待发箱 | 后端短暂不可用时，消息可能在落盘前丢失；系统又不补历史记录 | 使用本地缓存的监听状态先判断；接收后先持久化事件，再异步上报；限制条数和总字节，并在面板显示积压 |
| P0-2 | 事件接口在保存事件后同步触发 AI 生成 | AI 请求可能超过插件请求超时，引发重复上报和状态混乱 | 事件接口只校验、入库、排队并快速返回；后端调度器在防抖窗口结束后生成草稿 |
| P0-3 | 自动代聊发送结果没有持久待发箱 | 插件或后台重启、回传失败时，平台已发送但后端状态可能永久停留 | 新增独立的代聊结果待发箱；先本地记录结果再回传；仅重试结果上报，不重复平台发送 |
| P0-4 | 发送命令不携带领导页任期 | 标签页切换后，旧领导页可能执行延迟命令 | 命令增加 `leader_epoch`；Content 与 Main World 在发送前再次核对标签页和任期 |
| P0-5 | 暂停、人工接管未完整取消待发送动作 | 用户手动回复后，已确认的发送任务仍可能继续发送 | 状态切换与发送任务取消放在同一事务；已开始发送的任务转人工核对边界 |
| P0-6 | 缺少加密联系人标识时回退到展示名称 | 展示名称不是可靠协议身份，可能发错对象或发送失败 | 不再用名称代替身份；身份不完整时会话标为不可发送，并引导用户刷新目标会话 |
| P0-7 | 联系人缓存缺少账户、岗位和有效期边界 | 多账户、岗位变化或长时间运行时可能复用错误上下文 | 按账户、对方身份、岗位组合缓存；增加有效期和冲突检测；冲突时阻止发送 |
| P0-8 | 自动代聊真实平台发送从未验证 | 协议、页面状态或回执细节可能与模拟测试不同 | 先完成非发送验证，再由用户指定联系人进行一次受控实测；结果不明确时停止并人工检查 |

### 5.2 P1：当前优化范围

| 编号 | 问题 | 优化目标 |
| --- | --- | --- |
| P1-1 | 连续消息会反复创建任务，三秒聚合没有真正落地 | 同一会话连续文本在固定防抖窗口内合并为一次生成输入，并保存参与生成的消息编号 |
| P1-2 | AI 只返回纯文本 | 使用结构化输出：处理决定、回复正文、使用事实、风险提示、是否必须人工补充 |
| P1-3 | 敏感内容主要依赖提示词 | 对联系方式、薪资、面试时间、到岗时间、承诺和未知事实做确定性提示，显示在确认卡片中 |
| P1-4 | 身份从不完整变完整后可能创建两个会话 | 增加身份补全与合并规则；不能唯一判断时进入待确认状态，不自动合并 |
| P1-5 | “可发送”和“岗位资料完整”混用 | 分离平台身份状态、岗位关联状态和上下文完整状态，桌面端给出准确提示 |
| P1-6 | 列表、详情和消息一次性全量加载且每三秒刷新 | 增加服务端分页、筛选和消息游标；运行状态轻量轮询，详情按选中会话增量刷新 |
| P1-7 | 待处理数量实际只有零或一 | 计算真实待处理数，或在无法计算前把文案调整为“待处理” |
| P1-8 | 临时补充说明和编辑草稿可能串会话或丢失 | 按会话保存本地编辑态和临时说明；切换前保存，恢复时校验草稿版本 |
| P1-9 | 确认按钮只解释少量阻塞原因 | 覆盖草稿为空、版本过期、暂停、接管、身份不完整、插件离线、无领导页和发送关闭等原因 |
| P1-10 | 桌面端领导页信息可能来自其他账户 | 运行状态按账户返回领导页，所选会话只展示其账户的插件状态 |
| P1-11 | “立即检查”与实际能力描述不一致 | 实现安全的已知联系人核对；若当前阶段不做核对，则将按钮改为准确的“处理待生成任务” |
| P1-12 | 事件表重复保存完整消息正文 | 事件表只保留处理元数据，正文以消息表为唯一业务存储位置 |
| P1-13 | 插件轮询错误被吞掉，用户无法判断异常 | 保存最近成功时间、最近错误、事件积压和结果积压，并在展开面板中展示 |
| P1-14 | 已发送消息的回声去重只保存在内存中 | 持久关联 `client_mid`、动作编号和终态，在合理时限内完成回声对账 |
| P1-15 | MQTT 客户端断线后的生命周期不够清晰 | 断开后结束旧实例并按状态重建；发送前检查连接和目标身份 |

### 5.3 待真实环境确认的问题

- MQTT 发布时的保留标志当前沿用既有协议设计。在取得真实协议证据前不调整，只在受控实测中确认。
- BOSS 页面结构、聊天协议字段和登录态可能随平台变化，真实验证时要记录实际字段差异并以兼容解析修正。
- WebSocket 观察器能否稳定覆盖页面首次加载、断线重连和多个聊天窗口，需要在不同页面状态下验证。

## 六、目标架构

自动打招呼和自动代聊继续使用同一插件连接，但保持两条独立业务通道：

```text
                 ┌──────────────── 插件公共外壳 ────────────────┐
                 │ 连接状态、页面状态、面板、兼容消息桥         │
                 └──────────────┬────────────────┬──────────────┘
                                │                │
                 已验证通道      │                │ 自动代聊通道
                                ▼                ▼
                      默认打招呼执行器      消息观察与事件待发箱
                             │              联系人身份协调
                             │              领导页与发送动作
                             ▼                    │
                    原有后端执行状态机             ▼
                                         自动代聊后端状态机
                                                │
                                                ▼
                                         桌面端人工确认
```

共享层只增加通用展示字段和兼容消息类型。自动代聊的事件、发送结果、领导页任期和故障恢复使用独立存储键与状态，不复用自动打招呼的业务状态。

## 七、详细实现方案

### 7.1 插件：消息观察与可靠上报

1. Main World 只负责解析 BOSS 消息和提取可验证身份，不承担后端状态决策。
2. Content 收到消息后先完成格式校验、来源判断和消息编号去重。
3. 已开启监听时，先把事件写入本地 `chat event outbox`，写入成功后再尝试调用后端。
4. 后端确认接收后删除对应本地事件；网络失败保留并指数退避，但设置最大退避时间。
5. 待发箱设置条数和容量上限。接近上限时在面板警告；达到上限后停止接收新的自动代聊事件并明确提示用户处理，避免静默覆盖旧消息。
6. 插件启动、网络恢复和定时轮询时继续冲刷待发箱。
7. 事件幂等键使用稳定的 BOSS 消息标识；没有稳定标识的消息标记为不可自动处理，避免根据易变文本构造错误去重。

### 7.2 后端：快速入站与防抖生成

事件接口职责收敛为：

1. 校验执行器、账户、消息结构和幂等键。
2. 在事务中保存消息并更新会话版本。
3. 将同会话未开始生成的任务标记为过期。
4. 创建或更新一个 `pending` 回复任务，写入 `due_at = latest_message_at + 3 秒`。
5. 快速返回已接收结果，不等待大模型。

后台调度器在到期后读取防抖窗口中的连续招聘方文本，生成一次草稿。附件、图片、系统通知和解析不完整的消息保留在会话中，并把任务转为人工处理，不把未知内容猜成文本。

### 7.3 AI 草稿契约

建议使用以下逻辑结构，具体字段沿用项目现有命名风格：

```json
{
  "decision": "reply | manual | ignore",
  "reply_text": "建议回复内容",
  "facts_used": ["本次回复实际引用的已知事实"],
  "warnings": ["salary", "interview_time"],
  "requires_user_input": false,
  "reason": "简短原因"
}
```

后端必须验证输出：

- `reply` 必须有非空正文。
- 未知薪资、联系方式、面试时间、到岗日期和确定性承诺不能由模型自行补全。
- `manual` 进入待用户补充状态，不生成可直接确认的发送动作。
- `ignore` 只表示当前不建议回复，仍保留用户手动生成和编辑能力。
- `facts_used` 和警告用于桌面端解释，不直接拼接到发送正文。

### 7.4 会话身份与上下文

会话需要区分三个维度：

- `identity_state`：BOSS 账户、对方加密身份、岗位加密身份是否满足平台发送要求。
- `job_context_state`：是否已经关联 FineJob 本地岗位、公司和招聘方资料。
- `conversation_state`：监听中、暂停、人工接管或不可支持。

身份补全过程：

1. 新消息只有对方身份时先建立待补全会话。
2. 后续获得岗位身份后，根据同账户、同对方、时间邻近和唯一候选关系补全。
3. 出现多个候选岗位或身份冲突时不自动合并，桌面端显示“身份待确认”。
4. 只有平台发送身份完整且无冲突时才能创建发送动作。
5. 本地岗位没有关联时仍可生成保守草稿，但必须提示上下文有限；涉及岗位事实的问题转人工补充。

### 7.5 发送动作与故障恢复

发送状态建议保持单向流转：

```text
queued → leased → dispatching → sent
   │        │          │
   ├────────┴──────────┴→ cancelled
   └────────────────────→ failed
                         → unknown
```

关键规则：

- `queued` 和过期的 `leased` 可重新领取。
- 进入 `dispatching` 后不得再次下发平台发送请求。
- `dispatching` 超过截止时间且没有可靠结果时转为 `unknown`，等待人工核对。
- 插件收到平台结果后先写入 `chat result outbox`，再更新后端。
- 结果上报可重复，后端按动作编号幂等处理。
- `leader_epoch`、目标标签页和执行器身份必须与领取时一致。
- 暂停、人工接管、新消息导致确认过期时，事务内取消仍未进入平台发送的动作。

### 7.6 人工接管与新消息竞争

以下事件都会使旧草稿或旧发送许可失效：

- 招聘方又发来新消息。
- 用户在 BOSS 页面手动发送消息。
- 用户在桌面端暂停或选择人工接管。
- 会话身份、关联岗位或关键上下文发生变化。

后端以会话版本作为最终判断。桌面端确认时提交当前任务、基准消息和版本；动作领取和插件执行前还要再次校验。这样可以覆盖“确认按钮点击后又收到新消息”的竞争窗口。

### 7.7 API 调整

在保持已有接口可用的前提下，增加以下能力：

- 会话列表：支持状态、工作状态、关键词、账户、游标和分页大小。
- 会话详情：返回身份状态、上下文状态、确认阻塞原因、当前任务摘要和账户级插件运行状态。
- 消息列表：支持 `before/after` 游标和分页，避免每次加载全部历史。
- 事件接收：只负责入库和排队，返回接收数量、重复数量和拒绝原因。
- 回复确认：返回具体阻塞码，桌面端可直接转成操作提示。
- 发送动作：增加领导页任期和派发截止时间。
- 运行状态：区分监听、生成和发送开关，并按账户返回领导页、最近心跳和插件异常。
- 立即处理：明确为处理已到期回复任务；联系人核对另设语义清晰的操作。

所有新增响应字段为兼容扩展；旧字段在桌面端和插件完成迁移前继续保留。

### 7.8 数据调整

优先在现有数据模型上增加必要字段：

- 会话：身份状态、上下文状态、身份冲突原因、最后入站时间。
- 回复任务：防抖截止时间、输入消息编号集合、结构化决定、使用事实和警告。
- 发送动作：领导页任期、派发截止时间、平台客户端消息编号、人工核对原因。
- 领导页：继续按账户保存，运行状态不再只返回全局最后写入者。
- 事件：只保存事件编号、处理状态、错误和时间等元数据，消息正文保存在消息表。

数据迁移要为现有记录提供可推导默认值，并先验证桌面端读取旧数据的兼容性。

## 八、桌面端自动代聊页面优化

### 8.1 页面信息架构

建议采用三段式结构：

```text
┌────────────会话列表────────────┬────────────聊天与处理区────────────┐
│ 搜索 / 状态筛选 / 账户          │ 招聘方、岗位、身份与运行状态       │
│ 待处理数量、最后消息、风险标记   │ 消息时间线                         │
│ 分页或继续加载                   │ 当前草稿、事实、警告、编辑与确认   │
└────────────────────────────────┴───────────────────────────────────┘
                         底部：暂停 / 接管 / 刷新
```

窄窗口下改为会话列表与详情两级页面，避免把所有信息挤在同一屏。

### 8.2 信息分层

首屏只突出用户当前需要判断的内容：

1. 对方最新消息和到达时间。
2. AI 建议回复及生成状态。
3. 已使用事实与风险警告。
4. 是否可确认，以及不能确认的明确原因。
5. 插件是否在线、对应账户是否有领导页、发送功能是否开启。

消息编号、会话版本、动作编号、错误原文等放到可展开的诊断区。

### 8.3 编辑和确认体验

- 每个会话独立保存编辑中的正文和临时说明。
- 切换会话时自动保存本地编辑态；服务端草稿版本更新后提示用户选择恢复或使用新草稿。
- 确认按钮附近显示本次发送对象、岗位、最后一条基准消息和风险提示。
- 新消息到达后立刻禁用旧确认，并提供“基于最新消息重新生成”。
- 后端返回阻塞码时展示可执行动作，例如“打开 BOSS 对应聊天以补全身份”“等待插件上线”“恢复发送功能”。
- 发送状态使用“排队中、插件处理中、已提交、失败、结果待核对”，不把“已提交”描述为对方已收到。

### 8.4 刷新策略

- 运行状态使用轻量轮询。
- 会话列表只拉取有变化的摘要或当前页。
- 当前会话按消息游标增量更新。
- 用户正在编辑时，后台刷新不能覆盖输入框。
- 页面隐藏后降低轮询频率，恢复可见时立即刷新一次。

## 九、插件面板优化

### 9.1 缩小与展开

插件面板增加两种模式，解决当前固定大面板遮挡页面的问题。

折叠态建议仅显示：

```text
FineJob  ●已连接  队列 2  [暂停] [展开]
```

展开态按以下顺序展示：

1. 顶栏：FineJob、连接状态、缩小按钮。
2. 风险区：登录失效、页面不匹配、后端离线、待发箱积压。
3. 自动打招呼区：显示完整任务列表和执行状态。
4. 自动代聊区：监听/生成/发送开关、领导页、事件积压、结果积压和发送任务。
5. 执行任务区：岗位、招聘方、执行阶段和最近结果。
6. 诊断区：最近成功时间与可展开错误详情。

### 9.2 面板交互细节

- 记住折叠状态和面板位置。
- 支持拖动，重新打开时把位置限制在当前可视区域内。
- 设置最大高度，内容区独立滚动，不遮住整个 BOSS 页面。
- 窗口尺寸变化时自动把面板移回可见范围。
- 关键按钮保持稳定位置，并区分“暂停执行”和“停止发送”等不同含义。
- 颜色之外同时使用文字和图标表达状态。
- 默认展示用户可理解的错误，技术细节放在诊断区。
- 自动打招呼区调用执行器控制接口，展示任务状态和执行结果。

### 9.3 插件状态模型

面板需要分别展示：

- 插件与后端连接状态。
- BOSS 登录和当前页面状态。
- 自动打招呼运行状态与任务列表。
- 自动代聊监听、生成、发送状态。
- 当前账户是否持有领导页。
- 新消息事件待发箱数量。
- 发送结果待发箱数量。
- 最近一次状态上报、成功上报和失败原因。

不要把“插件在线”“自动代聊监听开启”“允许发送”合并成一个状态，避免用户误判。

## 十、实施阶段

### 阶段 0：建立保护基线（已完成，2026-08-31）

目标：明确改动边界，确保后续能证明自动打招呼没有回归。

- 固化现有自动打招呼、插件客户端和后端执行器测试命令。
- 为共享消息分流补充回归用例：旧打招呼命令仍进入原分支，旧结果仍写入原待发箱并回传。
- 增加插件面板基础组件测试，覆盖旧控制按钮调用。
- 记录当前自动代聊状态机和接口样例，作为迁移基线。

实际完成：

- 默认招呼执行改为完整任务列表同步、岗位身份匹配和执行结果回传。
- 新增共享命令分流回归，验证自动打招呼与自动代聊命令保持独立并按原顺序交给 Main World。
- 插件生产构建已覆盖 Vue 面板编译；面板交互组件测试随阶段 5 的界面重构统一完善，当前未增加新的测试依赖。
- 后端默认招呼执行器接口与任务状态需要按 `test.md` 重新核对。

### 阶段 1：修复消息不丢失和接口超时（已完成，2026-08-31）

目标：观察到的新消息先可靠保存，后端快速接收。

- 调整事件本地持久化顺序。
- 拆分事件入库与 AI 生成。
- 落实三秒防抖和连续消息聚合。
- 增加事件待发箱容量、重试和可见状态。
- 添加断网、后端超时、重复事件和插件重启测试。

实际完成：

- 插件持久保存最近一次成功取得的自动代聊运行开关，后端离线时继续按本地监听状态接收消息。
- 消息经过领导页校验后先写入本地事件待发箱，再异步尝试上传；上传失败时保留并由后续轮询恢复。
- 事件待发箱设置 200 条和 2 MiB 两个容量边界，达到边界后保留旧消息、拒绝新消息并提供明确错误状态。
- 插件状态面板增加自动代聊监听和待上传数量的最小展示，详细面板重构仍归阶段 5。
- 后端事件接口只进行校验、幂等入库和任务排队，固定返回 `generated = 0` 和 `processing_deferred`，不再同步等待大模型。
- 回复任务增加生成到期时间和输入消息编号集合；即时模式采用三秒防抖，同一会话窗口内的连续文本更新同一个待生成任务。
- 后台调度器按秒检查到期任务；定时和手动触发模式继续遵守原有运行设置，“立即检查”可跳过防抖并由用户主动生成。
- 新增本地监听缓存、后端离线先落盘、容量不静默覆盖、连续消息聚合和数据库字段测试。

阶段验证：

- 插件类型检查通过。
- 插件 7 个测试文件、31 项测试通过，其中自动打招呼测试 4 项继续通过。
- Chrome MV3 生产构建通过。
- 后端数据库、自动代聊 API、自动打招呼 API 和执行器定向测试共 24 项通过。
- 未执行真实 BOSS 自动代聊发送。

### 阶段 2：修复发送状态机（已完成，2026-08-31）

目标：避免重复发送、错标签页发送和人工接管后继续发送。

- 增加领导页任期验证。
- 增加自动代聊结果待发箱。
- 补齐 `dispatching` 截止和 `unknown` 收口。
- 暂停、接管和手动回复事务化取消动作。
- 持久保存客户端消息编号并处理回声对账。

实际完成：

- 自动代聊发送命令携带领导页任期，Content 入队和 Main World 取命令时都会拒绝过期任期。
- 增加独立发送结果待发箱；平台结果先在插件本地持久化，再异步回传后端，结果上报失败不会再次触发平台发送。
- 发送动作保存领导标签页、任期、派发截止时间和客户端消息编号；进入 `dispatching` 后超时统一转为 `unknown`，不会重新领取。
- 后端在开始派发前再次核对账户级领导页；迟到的可靠结果允许把 `unknown` 收口为明确终态。
- 暂停、人工接管和手动回复会取消未派发动作；已经派发的动作转入人工核对边界。
- 自身消息回声通过客户端消息编号与已保存的助手消息对账，避免误判为人工接管或重复创建任务。

阶段验证：插件发送结果恢复、领导页过期命令拒绝和后端暂停、派发超时、消息回声用例通过；未执行真实平台发送。

### 阶段 3：完善身份与草稿质量（已完成，2026-08-31）

目标：发送对象可验证，草稿风险可解释。

- 移除展示名称身份回退。
- 完成联系人缓存隔离、有效期和冲突处理。
- 增加身份补全与会话合并规则。
- 接入 AI 结构化输出和确定性内容警告。
- 分离身份、岗位上下文和会话状态。

实际完成：

- 移除用展示名称替代加密联系人身份的回退；平台身份不完整时明确阻止确认发送。
- 联系人上下文按账户、对方身份和岗位隔离，设置五分钟有效期与数量上限。
- MQTT 发送前校验账户、对方、岗位和安全身份；发送过程中等待发布确认，发布结果不明确时返回 `unknown`。
- 不完整会话在后续取得完整岗位身份时按唯一关系补全，避免重复会话；无法唯一判断时保持受限状态。
- 会话分别返回平台身份状态和岗位关联状态，桌面端不再把两者混为一个“可发送”判断。
- AI 回复任务保存结构化决定、正文、使用事实、警告、是否需要用户补充和原因；联系方式、薪资、面试、到岗和承诺等内容使用确定性分类提示。
- 编辑草稿和确认发送时重新计算内容警告；同一任务正在生成时不重复启动生成。

阶段验证：身份补全、内容分类、结构化任务字段、发送前校验和相关 API/数据库回归通过。

### 阶段 4：优化桌面端自动代聊页面（已完成，2026-08-31）

目标：让用户快速找到待处理会话，并安全完成编辑和确认。

- 改造列表、详情、风险卡片和确认阻塞提示。
- 实现每会话独立编辑态。
- 增加分页、筛选、增量加载和轻量轮询。
- 明确“已提交”和“结果待核对”等状态文案。

实际完成：

- 会话列表支持关键词、状态和账户筛选以及继续加载；详情默认限制最近消息并提示是否截断。
- 每个会话分别保存临时说明与编辑正文，切换会话、后台刷新和重新生成时按草稿版本保护用户输入。
- 页面展示 AI 决定、使用事实、内容警告、身份状态、岗位关联状态和可执行的确认阻塞原因。
- 运行状态按账户选择对应领导页；插件离线、无领导页、暂停、接管、身份不完整、草稿过期和发送关闭均有明确提示。
- 轮询只刷新运行状态、列表与当前详情，页面隐藏时暂停，恢复可见后继续；不会用全页加载态打断编辑。
- “立即检查”调整为“处理待生成任务”，“已提交”明确表示 MQTT 发布已确认，不表示招聘方已读。
- 生产页面在 1440 像素视口完成检查，工作区无横向溢出，筛选区、回复区和运行控制布局正常。

阶段验证：桌面端自动代聊定向测试 9 项、桌面端完整测试 124 项和生产构建全部通过。

### 阶段 5：优化插件面板（已完成，2026-08-31）

目标：支持缩小、拖动和清晰状态展示。

- 实现折叠态、展开态和位置记忆。
- 重排自动打招呼与自动代聊信息。
- 展示待发箱、领导页和最近错误。
- 验证不同窗口尺寸和 BOSS 页面区域不会被持续遮挡。

实际完成：

- 面板增加折叠与展开模式，折叠后只保留连接、队列和控制入口，解决原面板不能缩小的问题。
- 顶栏支持拖动；折叠状态和位置保存到插件本地存储，重新打开和窗口缩放时自动限制在可视区域。
- 展开内容设置最大高度和独立滚动，窄窗口自动缩小宽度，减少对 BOSS 页面内容的持续遮挡。
- 自动打招呼与自动代聊分区展示；任务列表和发送任务分别展示，暂停、恢复和返回审核调用对应控制接口。
- 自动代聊区显示监听、事件积压、结果积压、最近成功时间和错误，方便判断消息是否已可靠上报。

### 页面问题修复（已完成，2026-09-01）

- 移除自动代聊页面首版历史聊天读取限制提示。
- 移除聊天详情中的“最新消息：时间”展示行。
- 移除“平台身份完整/待补全”标签，保留本地岗位关联状态。
- 移除右侧聊天详情中的“【new】/【已送达】”消息状态标记，左侧会话列表继续保留状态标记。
- 右侧聊天详情无岗位标题时显示“本地岗位未关联”标签，并增加“更新岗位”按钮；左侧会话列表保持原有展示。
- 将“打开 BOSS 沟通页”入口移动到自动代聊运行设置区。
- 状态同时使用文字与颜色表达，主要信息和诊断信息分层呈现。

### 历史聊天按需获取（已完成，2026-09-02）

- “获取消息”与“更新”均只读取当前会话最新 20 条，完成本地去重同步并保存是否还有更早消息。
- 聊天详情在存在更早记录时展示“获取更多”；每次点击使用 BOSS 返回的消息游标再读取 20 条，直到没有更多记录。
- 会话详情展示已经获取的全部本地聊天记录；批量更新后续只使用首次读取路径，不触发“获取更多”。

### 岗位状态和聊天岗位补录（已完成，2026-09-02）

- 自动代聊右侧已区分“查看岗位”和“更新岗位”：已有历史岗位显示“查看岗位”，打开历史采集详情；未关联岗位显示“更新岗位”。
- “更新岗位”读取左侧会话的 `encryptJobId`，打开 `job_detail` 页面，创建历史岗位记录并启动独立详情任务，不调用聊天列表刷新逻辑。
- 新增独立的 `boss_job_detail.py` 详情获取解析脚本，原有 `boss_cdp_raw.py` 和普通批量采集流程完整保留。
- 新脚本优先读取 BOSS 详情接口数据，页面数据作为补充，解析岗位名、薪资、公司规模、行业、融资阶段、地点、经验、学历和 JD。
- 聊天岗位详情完成后回写历史岗位的 `title`、`salary`、`company_scale`、`company_industry`、`company_stage`、`location`、`experience`、`degree` 等字段；缺失字段保留原值或为空。
- 历史岗位按 `encrypt_job_id` 去重；底层重复写入时沿用原历史记录，保持岗位关联稳定。
- 聊天补录岗位的筛选状态固定为 `pass_for_human`，历史采集页面以绿色“跳过”显示。
- 历史采集页面增加“投递状态”和“投递建议”列，并在详情抽屉中显示投递阶段。
- 投递状态统一为 `pending_greeting`、`pending_application`、`communicating`、`rejected` 和 `null`，页面分别显示“待打招呼”“待投递”“沟通中”“已被拒绝”和空状态。
- 状态依据 FindJob 建议、`BOSS_DEFAULT_GREETING` 成功动作和我方“附件状态更新”消息判断；“已被拒绝”只通过聊天页面按钮人工设置。
- 旧数据库中的 `cleared` 批量迁移为 `pending_greeting`；旧 `applied` 按已有附件消息迁移为 `communicating` 或 `pending_application`，新写入不再使用旧枚举。
- 新增历史岗位详情查询接口，支持“查看岗位”从自动代聊跳转后直接打开对应详情。
- 已清理实际运行数据库 `.local/app-data/backend-dev/knowledge-curator.db` 中岗位名为空的 2 条 `fj_boss_jobs` 记录；其岗位关联数据按数据库级联处理，聊天会话保留并解除岗位关联。

阶段核对：本轮按要求未运行测试；数据库清理前确认目标为 2 条，清理后空岗位名历史记录为 0 条。新脚本、聊天任务入口、字段回写入口和原脚本保留情况已完成代码核对。

### 阶段 6：联调与受控实测（非发送部分已完成，2026-08-31）

目标：证明完整链路在真实环境可用。

- 完成无真实发送的页面观察、事件入库、草稿生成和确认前校验。
- 验证多标签页领导切换、插件重启、后端短暂离线和桌面端切换会话。
- 用户指定测试联系人并在执行当时批准后，只执行一次真实发送。
- 对照 BOSS 页面、插件、后端和桌面端状态。
- 若结果不明确，标记 `unknown` 并停止，不重复发送。

实际完成：

- 已完成后端事件入库、防抖任务、结构化草稿、确认前阻塞、领导任期、发送结果恢复和多端状态展示的自动化验证。
- 已使用独立后端端口、独立 Electron 用户目录和生产构建完成桌面端非发送烟雾检查。
- 本次没有操作真实 BOSS 联系人，没有确认或执行真实发送。
- 单次真实发送保留为受控验收项：需要用户指定测试联系人，并在执行当时再次明确批准。

## 十一、预计文件影响范围

### 11.1 浏览器插件

- `boss-executor-extension/src/ui/StatusPanel.vue`
- `boss-executor-extension/src/ui/mount-status-panel.ts`
- `boss-executor-extension/src/entrypoints/content.ts`
- `boss-executor-extension/src/entrypoints/background.ts`
- `boss-executor-extension/src/entrypoints/boss/index.ts`
- `boss-executor-extension/src/finejob/chat-coordinator.ts`
- `boss-executor-extension/src/finejob/client.ts`
- `boss-executor-extension/src/finejob/types.ts`
- `boss-executor-extension/src/platform/boss/chat/observer.ts`
- `boss-executor-extension/src/platform/boss/chat/sender.ts`
- `boss-executor-extension/src/platform/boss/chat/contact-context.ts`
- 对应的消息桥和测试文件

其中 `client.ts`、BOSS 入口和公共消息桥只允许兼容扩展，必须由自动打招呼回归测试保护。

### 11.2 后端

- 自动代聊路由、服务、模型、数据访问和数据库迁移文件。
- 后台调度入口与应用生命周期。
- 自动代聊 API、服务和数据库测试。

默认招呼执行器相关服务、路由和数据结构本轮已按 `test.md` 完成错误概念清理。

### 11.3 桌面端

- 自动代聊页面 `BossChat.vue`。
- 对应 store、API、类型、策略和测试。
- 如有必要，拆出会话列表、状态卡片、草稿编辑器和风险提示组件，控制单个文件复杂度。

### 11.4 文档

实施完成后更新：

- 本执行方案的阶段状态、实际差异和验证结果。
- 用户操作文档中的自动代聊开启、人工确认、暂停/接管和异常处理说明。
- 插件面板说明和真实受控实测记录。

## 十二、测试方案

### 12.1 自动打招呼强制回归

每个阶段至少验证：

- 默认打招呼命令解析和路由。
- 列表页、详情页身份校验的现有行为。
- 完整任务列表同步、岗位匹配、执行结果回传。
- 插件暂停、恢复和停止控制。
- 后端执行器状态流转。
- 共享类型新增字段后，旧消息仍可解析。

这些测试失败时，本阶段不能进入合并和真实联调。

### 12.2 自动代聊单元与集成测试

- 事件先落盘后上报，重启后继续发送。
- 后端离线、超时、重复事件和乱序事件。
- 三秒内多条文本聚合，窗口外消息产生新任务。
- AI 输出格式错误、空正文、未知事实和风险警告。
- 会话身份补全、冲突、不完整和多账户隔离。
- 版本变化后确认失效。
- 暂停、接管、手动回复与动作领取并发。
- 领导页切换、旧任期命令拒绝和多标签页竞争。
- 平台发送成功、明确失败、超时和结果上报失败。
- `unknown` 状态绝不自动重发。
- 客户端消息回声正常去重。

### 12.3 桌面端组件测试

- 会话筛选、分页和选中状态。
- 每会话编辑态隔离。
- 新消息到达后旧草稿的提示和禁用。
- 所有确认阻塞原因与对应操作。
- 用户编辑过程中轮询不覆盖正文。
- 暂停、接管和恢复的界面状态。

### 12.4 插件面板测试

- 折叠、展开、拖动、位置保存和视口约束。
- 面板最大高度和内部滚动。
- 连接、页面、打招呼、代聊、领导页和待发箱状态分别显示。
- 自动打招呼原按钮仍调用原接口。
- 面板重挂载和页面路由变化后不重复创建实例。

### 12.5 真实环境验收顺序

1. 只观察消息，不开启生成和发送。
2. 开启生成，检查草稿和上下文，不确认发送。
3. 打开确认流程，验证版本失效和暂停。
4. 用户批准后对指定联系人执行一次发送。
5. 观察回声、后端终态、桌面端状态和重复发送情况。
6. 验证完成后再决定是否扩大使用范围。

## 十三、验收标准

### 13.1 功能验收

- 插件面板可缩小、展开和拖动，刷新后状态与位置合理恢复。
- 默认招呼按 `test.md` 的岗位匹配与结果回传流程执行。
- 新消息在后端暂时不可用时不会在本地静默丢失。
- 连续招聘方消息按防抖窗口生成一次草稿。
- 草稿可编辑，风险与使用事实可见，用户确认前不会发送。
- 新消息、暂停、接管或身份变化会使旧确认失效。
- 多标签页只有当前账户的有效领导页可以发送。
- 发送结果回传失败后可以恢复上报，不重复执行平台发送。
- 结果不明确时进入人工核对，不自动重试。

### 13.2 体验验收

- 用户能在一个首屏内看清“对方说了什么、建议回什么、有什么风险、为何能或不能发送”。
- 列表不会因会话增长而持续全量加载。
- 桌面端编辑内容不会因轮询或切换会话意外消失。
- 插件错误具有可操作提示，技术信息不干扰主要操作。
- “已提交”“已发送”“结果待核对”等文案与实际状态一致。

### 13.3 稳定性验收

- 插件重启、后端短暂离线和领导标签页切换后，事件与结果可恢复。
- 事件和结果待发箱有明确容量边界和可见告警。
- 同一消息、确认和结果的重复请求保持幂等。
- 自动代聊异常不会阻断默认招呼的任务匹配和结果回传。

## 十四、风险与取舍

- 不同步历史聊天可以降低平台读取复杂度，但插件未运行期间的消息无法补齐，因此本地事件先持久化是当前可靠性的关键。
- 保持人工确认会增加一次操作，但能控制错误事实、敏感信息和误发风险，符合当前未实测阶段。
- 发送进入 `dispatching` 后不自动重试可能产生少量人工核对任务，但能避免重复发送。
- 联系人身份不完整时阻止发送会降低覆盖率，但比使用展示名称推断身份更可靠。
- 分页与增量刷新增加少量接口工作，可以避免会话增多后桌面端持续变慢。

## 十五、二阶段候选优化任务：自动打招呼

以下内容只记录建议，本次自动代聊实施不执行，后续由用户单独决定：

1. 增加执行器重新配对和旧凭据失效的友好流程，避免多个旧插件实例长期保留。
2. 强化列表页与详情页岗位身份一致性校验，同时核对岗位编号、安全标识和招聘方身份。
3. 为真实 BOSS 页面样本补充自动打招呼回归夹具，提高页面变化时的发现能力。
4. 继续细化面板中的任务状态语义，避免把等待任务显示成正在执行。
5. 建立受控的自动打招呼回归流程，覆盖登录变化、页面不匹配、后端离线和结果恢复。

二阶段开始前需要重新确认范围。任何优化都以保持现有打招呼成功链路为首要验收条件。

## 十六、执行结论

用户定义的一期已经完成代码实现、自动化回归、生产构建和桌面端非发送界面验收，范围覆盖插件本身、自动代聊后端和桌面端自动代聊页面。插件已经支持缩小、拖动、位置恢复和分区状态展示；自动代聊已经补齐消息可靠入库、发送状态收口、领导任期、身份校验、结构化草稿、风险提示、列表分页和编辑态保护。

默认招呼执行已按 `test.md` 收敛为完整任务列表、岗位匹配和结果回传流程。二阶段默认招呼优化建议仅保留在本文第十五节，尚未实施。

当前唯一未完成的验收项是一次真实 BOSS 自动代聊发送。该操作涉及真实联系人，必须由用户指定对象并在执行当时再次批准；在此之前，一期实现保持人工确认和不自动重发的安全边界。

## 十七、执行器心跳连接调整（2026-09-02）

- 插件启动、配对成功、面板手动点击“心跳测试”、每项自动打招呼开始前，以及任务长期未回传状态时，执行一次心跳测试。
- 移除自动打招呼执行器的持续心跳循环；任务完成后继续进行下一轮岗位匹配。
- 心跳确认用于展示 FineJob 执行器连接状态，与 BOSS 页面是否打开分开处理；页面校验失败只暂停任务，不覆盖连接状态。
- 后端重启后先显示未连接并提供配对码；插件下一次心跳成功后恢复正常状态。

## 十八、桌面端主动心跳测试（2026-09-02）

- FineJob 桌面端启动时主动发起一次心跳测试；运行状态页提供“心跳测试”按钮。
- 桌面端请求经本机后端控制通道发送给已配对插件，插件执行一次心跳并回传结果。
- 插件面板中的“心跳测试”按钮继续保留，仍由插件主动执行一次心跳。
- 心跳成功后桌面端显示正常；心跳失败、插件未连接或超时后显示未连接并生成配对码。
- 控制通道使用一次性命令和回执，不新增数据库状态字段，不使用心跳轮询。

## 十九、插件断开连接与跨域修复（2026-09-02）

- FineJob 运行状态页增加“断开连接”按钮，插件面板同步增加“断开连接”按钮。
- 断开操作暂停执行器、使现有插件凭证失效、关闭控制通道并清理插件本地凭证；岗位、任务和历史记录保留。
- 断开后需要重新生成配对码并在插件面板重新配对。
- 修复多次配对后再次断开时，旧断开记录的失效凭证值重复导致数据库唯一约束报错的问题。
- 后端允许 Chrome 插件来源访问本机 FineJob API，修复插件请求 OPTIONS /api/fine-job/boss-chat/runtime 返回 400 的问题。

## 二十、移除自动代聊轮询（2026-09-02）

- 插件协调器取消 2 秒一次的处理定时器，不再周期性请求 `/api/fine-job/boss-chat/runtime`；运行配置仅在插件协调器启动时读取一次。
- 代聊消息、标签页身份和发送结果等业务事件负责触发本地处理，待发事件和发送结果继续保存在本地等待后续业务事件恢复上传。
- 后端移除每秒扫描代聊任务的常驻调度器，事件入库或运行配置更新后按任务到期时间安排一次性生成回调。
- 手动生成接口保留，发送动作的超时收口继续在领取动作和状态查询等实际业务请求中执行。

## 二十一、插件打包错误修复（2026-09-02）

- 修复插件客户端心跳异步函数缺失 `try` 结构导致的 TypeScript 语法错误。
- 修复控制通道状态判断的 TypeScript 类型不匹配。
- 类型检查和 Chrome MV3 WXT 生产构建均已通过。
