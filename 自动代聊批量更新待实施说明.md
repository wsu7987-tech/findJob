# 自动代聊批量更新实现说明

## 目标

在“自动代聊”页面完成联系人列表刷新后，集中处理需要同步的聊天记录，并为缺少详情的关联岗位补充岗位信息。

本功能每次最多处理 20 个聊天会话。

## 已确认的聊天记录规则

1. 本地没有聊天消息时，显示“获取消息”，读取该会话最新 20 条消息并更新本地。
2. 联系人列表发现 BOSS 最新消息尚未进入本地时，显示“更新”，读取最新 20 条消息并去重更新本地。
3. BOSS 历史接口返回 `hasMore` 时，聊天详情显示“获取更多”；用户每次点击后再读取更早的 20 条消息，直到没有更多记录。
4. “获取消息 / 更新”与“获取更多”是独立动作。批量更新只执行“获取消息 / 更新”的最新 20 条同步，不执行“获取更多”。

## 批量更新流程

```text
点击“更新聊天列表”
→ 获取并保存聊天列表
→ 保存 BOSS 返回数组中的会话顺序
→ 统计待更新聊天与待采集岗位
→ 显示“批量更新聊天记录（最多 20 条）”
→ 用户启动批量更新
→ 逐条同步最新 20 条聊天记录
→ 判断关联岗位是否已完成详情采集
→ 按需采集岗位详情
→ 完成全部队列并隐藏进度区
```

## 待更新统计

- 待更新聊天：本地没有消息，或联系人列表标记为 `message_update_required` 的会话。
- 待采集岗位：会话关联岗位不存在，或历史岗位的 `detail_status` 不是 `completed`。
- 页面同时显示全部待处理数量和本次队列数量；本次队列严格按 BOSS 聊天列表顺序，最多选取 20 条。

建议展示：

```text
待更新聊天：47 条
本次批量：20 条
```

## 单个会话执行顺序

```text
找到已打开的 /web/geek/chat 标签页并附着
→ 获取当前会话最新 20 条聊天消息
→ 去重更新本地
→ 判断岗位详情是否已完成
→ 无需采集：等待 2–5 秒后进入下一会话
→ 需要采集：随机等待 1–3 秒，启动岗位详情采集
→ 等待岗位详情采集完成
→ 随机等待 2–5 秒
→ 进入下一会话
```

岗位详情采集继续复用现有历史岗位详情任务与状态轮询能力。

## 界面要求

- “更新聊天列表”完成后刷新会话列表与待处理计数，并严格按 BOSS 返回顺序展示。
- “本次批量”后提供 1–20 的数量计数器；批量任务按该数量执行。
- “批量更新聊天记录”按钮固定在统计栏右侧，任务创建和运行期间禁用。
- 有新消息待更新或本地尚无消息的会话在列表中显示红点。
- 没有岗位标题时显示“岗位未返回”标签；已有岗位名使用正文颜色显示。
- 批处理运行时显示进度区：总进度、当前联系人或岗位、当前阶段、聊天已完成数、岗位已完成数与失败数。
- 批处理结束后隐藏进度区，并刷新会话列表和统计数量。

## 实现范围

- 后端提供批量任务创建、状态查询和逐项执行能力。
- 桌面端提供统计、启动入口、进度条和完成后的刷新。
- 聊天最新消息读取、岗位详情任务和 BOSS 沟通页附着逻辑复用现有实现。

## 验收标准

1. “更新聊天列表”后统计数量与会话列表状态一致，并保持 BOSS 列表顺序。
2. 批量任务一次最多处理 20 条待更新聊天。
3. 每个会话只读取最新 20 条，不自动读取更早历史。
4. 缺少岗位详情的会话会进入岗位详情采集，已完成详情的岗位直接跳过。
5. 页面可观察到逐项进度，完成后进度区消失并显示最新数据。

## 实现记录（2026-09-02）

- 已新增批量统计、启动和状态查询接口；一次队列最多包含 20 条待更新聊天。
- 已实现后端串行执行：同步最新 20 条聊天消息后，按岗位 `detail_status` 判断是否启动详情采集。
- 已实现岗位采集前随机等待 1–3 秒、每个会话完成后随机等待 2–5 秒的执行节奏。
- 已在自动代聊页面加入待处理统计、批量更新按钮和运行进度区；任务结束后页面自动隐藏进度区并刷新统计和会话列表。
- 已保存 BOSS 聊天列表顺序并用于页面与批量队列；待更新会话显示红点。
- 已修正岗位标题回填：新建聊天岗位保留已知标题，详情采集后同步补齐关联会话标题。
- 已修正岗位名样式覆盖关系，已有岗位名使用正文颜色与普通字重显示。
- 本轮按用户要求未运行测试或构建。
